import { CountoDirective } from './counto.directive';

describe('CountoDirective', () => {
  it('should create an instance', () => {
    const directive = new CountoDirective();
    expect(directive).toBeTruthy();
  });
});
